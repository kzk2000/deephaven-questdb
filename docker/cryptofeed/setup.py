from setuptools import setup, find_packages

setup(name='src',
      version='0.1',
      description='Deephaven + QuestDB real-time streaming',
      author='kzk2000',
      packages=find_packages(),
)