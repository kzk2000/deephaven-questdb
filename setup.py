from setuptools import setup, find_packages

setup(name='dhquest',
      version='0.0',
      description='Deephaven + QuestDB on-disk storage',
      author='kzk2000',
      packages=find_packages(),
)
